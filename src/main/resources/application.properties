spring.application.name=SPRINGWEB

server.port=8081
server.servlet.context-path=/spring

##### MySQL
#################### DataSource Configuration ##########################
# Make sure SSL settings are correct for your environment
spring.datasource.url=jdbc:mysql://localhost:3306/spring-web?useLegacyDatetimeCode=false&serverTimezone=Asia/Jakarta&useSSL=true&allowPublicKeyRetrieval=true
spring.datasource.username=ems
spring.datasource.password=BT20030411bt@
spring.jpa.hibernate.ddl-auto=update

# Enable Hibernate SQL logging (optional, only for development)
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Disable caching for development
spring.thymeleaf.cache=false

server.error.whitelabel.enabled=false
server.error.path=/error
spring.mvc.throw-exceptions=true

# Logging
#logging.level.org.springframework.security=DEBUG
#logging.level.org.springframework.web.filter=DEBUG

logging.file.path=/logs
# logging.level.root=ERROR  # Uncomment if you want to change log level

# File Uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

# URL for accessing the app
myurl=http://localhost:8081/spring
